# Core runtime (M0+). pyproject.toml is the source of truth; this mirrors the
# core dependencies for convenience. Install the package editable instead:
#   .venv\Scripts\python.exe -m pip install -e .
# Optional extras (installed per milestone):
#   .venv\Scripts\python.exe -m pip install -e ".[data]"   # M1.5 data providers
#   .venv\Scripts\python.exe -m pip install -e ".[api]"    # M3 FastAPI surface
#   .venv\Scripts\python.exe -m pip install -e ".[dev]"    # tests / notebooks
openai>=1.40.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0
